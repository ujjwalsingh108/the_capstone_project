# Infrastructure

This folder contains deployment-specific assets for cloud and production environments.

Suggested subfolders:

- `terraform/` for cloud infrastructure as code
- `kubernetes/` for manifests and overlays
- `helm/` for packaging if you choose Helm
- `docker/` for container-specific support files
